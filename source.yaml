apiVersion: source.toolkit.fluxcd.io/v1beta1
kind: GitRepository
metadata:
  name: dwk-repo
  namespace: flux-system
spec:
  interval: 10m
  url: https://github.com/rhiskk/kubernetes-hy
  ref:
    branch: main
